[project]
name = "bench"
version = "0.1.0"
description = "pg_vectorize HTTP-server benchmark"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "beir>=2.0.0",
    "httpx>=0.27.0",
    "psycopg[binary]>=3.1.0",
    "sentence-transformers>=3.0.0",
    "numpy>=1.24.0",
    "pytrec_eval>=0.5",
    "tabulate>=0.9.0",
]
